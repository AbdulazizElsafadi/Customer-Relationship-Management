import { SafeAreaView, ScrollView } from "react-native";
import RegionsList from "../features/RegionsList";

const RegionsListScreen = () => {
  return (
    // <SafeAreaView>
    // <ScrollView>
    <RegionsList />
    // </ScrollView>
    // </SafeAreaView>
  );
};

export default RegionsListScreen;
